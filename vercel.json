{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter admin build && rm -rf dist && mkdir -p dist && cp -R apps/admin/dist/. dist",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
