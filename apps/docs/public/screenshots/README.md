# Screenshots

PNGs captured from the iOS simulator via Maestro
(`apps/mobile/.maestro/docs-site-screenshots.yaml`). Filenames must match
the `screenshot` field in `docs-site/content/features.ts`.

This directory is intentionally tracked even when empty so the public path
exists at deploy time; the docs site renders a placeholder for any feature
whose screenshot file is missing.
