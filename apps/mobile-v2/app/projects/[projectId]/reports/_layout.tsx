/**
 * Reports route layout — wraps report list and detail screens.
 */
import { Stack } from "expo-router";

export default function ReportsLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
