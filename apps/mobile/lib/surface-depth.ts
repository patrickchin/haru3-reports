/**
 * Re-export shim. The canonical surface-depth tokens live in
 * `@harpa/report-ui/tokens`. Edit them there, not here.
 */
export {
  getSurfaceDepthStyle,
  type SurfaceDepth,
} from "@harpa/report-ui/tokens";
