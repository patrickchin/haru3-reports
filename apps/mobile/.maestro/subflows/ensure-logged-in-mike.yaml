# Shared subflow: ensure the app is logged in as Mike Torres on the projects screen.
# Clears app state first to guarantee an independent, consistent session.
appId: com.harpa.pro
---
- runFlow: ensure-logged-out.yaml

- tapOn:
    id: "demo-user-0"

- extendedWaitUntil:
    visible: "Projects"
    timeout: 10000
