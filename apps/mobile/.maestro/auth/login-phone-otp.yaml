# Login: Phone OTP happy path (Mike Torres)
appId: com.harpa.pro
tags:
  - auth
  - positive
  - smoke
---
- runFlow: ../subflows/ensure-logged-out.yaml

# Pre-login screen exposes server-info; assert it before signing in
- assertVisible:
    id: "server-info"

- runFlow: ../subflows/signup-or-login-mike.yaml

- assertVisible: "Projects"
